# Leave empty for local development and the standard Vercel deployment.
# Vite proxies /api locally, and api/[...path].js proxies /api in production.
# Set only when intentionally calling a different public backend directly.
VITE_API_URL=
